<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.awningmanufacturer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OfferEmail"></activity>
        <activity android:name=".SccRegistrationSeller" />
        <activity android:name=".SccRegistrationBuyer" />
        <activity android:name=".Website" />
        <activity android:name=".SccRegistration" />
        <activity android:name=".Group" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".Videos" />
        <activity android:name=".Contacts" />
        <activity android:name=".offers" />
        <activity android:name=".Overview" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>